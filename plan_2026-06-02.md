## 実装設計書

- 作成日: 2026-06-02
- 選択プラン: C（網羅）
- 依頼の要約: 記録画面のカテゴリ別タブで全品番をスクロール表示できるようにし、履歴タブで行をタップすると品番の全文・名前・詳細を表示するモーダルを追加する。

### 概要
記録画面の品番グリッドの高さ上限を広げてカテゴリ別でも全品番が見えるようにする（修正1）。履歴タブは行タップで詳細モーダルを開く方式に変更し、品番が途中で切れて読めない問題を解消する（修正2）。修正1と修正2は別々のコミットに分けて、1つずつ実装・確認する。

### 変更対象ファイル
| ファイル | 変更内容 | 変更規模 |
|---|---|---|
| index.html | 修正1: `.item-grid` の `max-height` を拡大（261行目付近の CSS 1箇所） | 小 |
| index.html | 修正2: 詳細モーダル用CSS追加 + `renderHistory()` に行タップ目印追加 + クリックハンドラ拡張 + 詳細表示関数の新規追加 | 中 |

### 機能仕様
#### 追加・変更する機能
- 修正1: 記録画面の品番グリッド（全て／カテゴリ別共通）の表示可能高さを広げる。タイル件数が多い場合は従来どおりスクロール可能。カテゴリ別でも所属する全品番が見える／スクロールで到達できる。
- 修正2: 履歴タブの各行（入出庫・棚卸しの両方）をタップすると詳細モーダルが開く。
  - 入出庫記録の詳細表示項目: 日時、入庫/出庫、品番（全文）、品名、種類、数量、手入力かマスタ登録か、取消/編集済の状態
  - 棚卸し記録の詳細表示項目: 日時、品番（全文）、品名、在庫数、棚卸しである旨
  - モーダルは既存の `showModal()` を流用。閉じるボタンと背景タップで閉じる（既存挙動）。
  - 編集・取消・削除ボタンは従来どおり右側に残し、ボタンタップ時は詳細モーダルを開かない（`stopPropagation` で制御）。

#### 変更しない機能
- 編集・取消・削除の各機能とその確認モーダル
- 記録の保存ロジック・スナップショット方式・集計・印刷・エクスポート
- 品番タイルのタップで記録入力に進む既存動線（記録画面側）

### ロジック・データ設計
変更なし（既存のデータ構造をそのまま読み取って表示するのみ）。

### UI / 画面設計
- 修正1: `.item-grid { max-height: 230px }` を、より多くのタイルが見える高さに拡大（`max-height: 50vh` 程度を想定。スマホ画面で品番グリッドが縦に伸びすぎない範囲で調整）。`overflow-y: auto` は維持。
- 修正2:
  - 履歴の行（`.rec-main` 領域）にタップ可能であることを示す軽いスタイル（`cursor: pointer`）を付与。
  - 詳細モーダルの中身は定義リスト風のレイアウト（項目名＋値）。品番は `word-break: break-all` で全文表示し折り返す。
  - 詳細モーダル内の品番は省略（ellipsis）しない。

### 実装ステップ
【修正1】（コミット1）
1. `index.html` 261行目 `.item-grid` の `max-height: 230px` を `max-height: 50vh` に変更。
2. 記録画面で「全て」「カテゴリ別」の両方で全品番が見える／スクロールできることを確認。
3. ユーザー確認後、GitHub Desktop でコミット。

【修正2】（コミット2）
4. CSS追加: `.rec-main` に `cursor: pointer`、詳細モーダル用の定義リスト風スタイル（`.detail-row` 等）を追加。
5. `renderHistory()` の各 `<li>` で `.rec-main` にタップ判定用の目印を付ける（行に既にある `data-rid` / `data-sid` を利用するため、`.rec-main` をタップ起点にする）。
6. `#hist-list` のクリックハンドラ（1418行目）を拡張:
   - 編集/取消/削除/棚卸し削除ボタン（`[data-act]` / `[data-sact]`）が押された場合は従来処理（先に判定して return）。
   - それ以外で `.rec-main` がタップされた場合、対象レコードを特定し詳細モーダルを開く。
7. 詳細表示関数を新規追加（入出庫用・棚卸し用の表示HTMLを組み立てて `showModal()` に渡す）。
8. 記録の詳細が正しく全項目表示されること、ボタン操作が干渉しないことを確認。
9. ユーザー確認後、GitHub Desktop でコミット。

### 確認・テスト項目
- [ ] 修正1: 「全て」タブで全品番がスクロール表示される（従来通り）
- [ ] 修正1: カテゴリ別タブで、そのカテゴリの全品番が見える／スクロールで到達できる
- [ ] 修正1: 品番が少ないカテゴリでもレイアウト崩れがない
- [ ] 修正2: 入出庫記録の行タップで詳細モーダルが開き、品番が全文表示される
- [ ] 修正2: 棚卸し記録の行タップで詳細モーダルが開く
- [ ] 修正2: 編集・取消・削除ボタンを押しても詳細モーダルが開かず、従来の動作になる
- [ ] 修正2: 取消済み・編集済みの記録で状態が正しく表示される
- [ ] 修正2: 長い品番（例: ABCDE-12345-XYZ）が折り返して全文表示される
- [ ] モバイル幅で詳細モーダルがはみ出さない

### リスク・注意点
- クリックハンドラの判定順序: `[data-sact]` → `[data-act]` → `.rec-main`（詳細）の順で判定し、ボタンが詳細モーダルを誤って開かないようにする。
- 棚卸し行には編集ボタンがなく削除のみ。詳細表示は棚卸し専用の項目構成にする。
- `max-height: 50vh` は機種により縦が長く感じる可能性。実機確認で値を微調整する余地あり。
- 既存のモーダル流用のため、新規の閉じる制御は追加しない（二重closeを避ける既存設計に従う）。

### 今回スコープ外
- 履歴の検索・絞り込み機能の変更
- 詳細モーダルからの直接編集（編集は従来の編集ボタン経由のまま）
- 記録入力動線・集計・印刷・エクスポートの変更
