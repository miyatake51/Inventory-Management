## 実装設計書

- 作成日: 2026-05-30
- 選択プラン: C（網羅）
- 依頼の要約: トラック運転手が社外倉庫の入出庫を10秒以内で記録する、自分専用の第二記録アプリを単一HTML（バニラJS・LocalStorage）で作る。

### 概要
電卓（calculator.html）の見た目と操作感を流用し、「品番タップ→数量テンキー→入庫/出庫→表示部で確認→金ボタンで確定」を10秒で回せる単一HTMLアプリ。過去記録は記録時点の品番名・種類を焼き付けて保存し（スナップショット方式）、マスタ編集・削除の影響を受けないようにして証拠性を守る。

---

### 確定した設計方針（実装前に確認したい4点・推奨案）
1. 記録の修正は「削除（完全消去・戻せない）」と「取消（記録は残し無効化＋理由をログに残す）」を分ける。証拠性のため取消を主とする。
2. 記録確定後は「数量だけクリア・品番選択は保持」を既定とする（同一品番の連続記録に対応）。
3. JSON読み込みは「全置換＋取り込み前に現状を自動でJSONバックアップ書き出し」を既定とする（マージは初版見送り）。
4. 初回起動時はサンプル品番を入れず、空の状態でマスタ登録へ誘導する。

---

### 変更対象ファイル
| ファイル | 変更内容 | 変更規模 |
|---|---|---|
| index.html（新規） | アプリ全体（HTML+CSS+JS を1ファイルに同梱） | 大 |
| concept_2026-05-30.md / README.md / CLAUDE.md | 触らない | なし |

---

### 機能仕様

#### 追加・変更する機能（プランC・すべて初版）
1. 品番マスタCRUD（各品番に種類カテゴリ。最大20件程度。論理非表示 active フラグ対応）
2. カテゴリCRUD（記録画面の種類タブ並び順を管理。削除時は所属品番を「未分類」へ寄せる）
3. 記録動線: 種類タブで絞り込み→品番タップ選択→数量テンキー→入庫/出庫選択→表示部で内容確認→確定。日時自動記録
4. リスト外品番の手入力（後でマスタへ昇格可能）＋表記ゆれ候補の警告
5. 履歴ビュー（新しい順・記録IDで安定ソート）＋編集・削除・取消（理由ログ）
6. グループビュー（種類別→品番別の現在差引）
7. 期間集計ビュー（開始〜終了日指定、品番ごとに入庫計/出庫計/差引。終了日は当日23:59:59まで含む）
8. 履歴の絞り込み検索（品番／期間）
9. 印刷/PDF（履歴＋期間集計を @media print で白地黒字に整形。ヘッダに対象期間・作成日時・件数を明記）
10. JSON書き出し/読み込み（復元）、CSV書き出し（BOM付きUTF-8）

#### 変更しない機能（スコープ外）
- ログイン・クラウド連携・多端末同期
- 写真添付
- オフライン専用対応（電波良好前提・依存ゼロのため結果的にオフラインでも動く）

---

### ロジック・データ設計

LocalStorage は接頭辞 `inv_` で衝突回避し、スライスごとに保存（記録追加のたびに全体を書き直さない）。

| キー | 用途 |
|---|---|
| inv_categories | 種類カテゴリ一覧 |
| inv_items | 品番マスタ |
| inv_records | 入出庫記録（生きている記録） |
| inv_voidlog | 取消ログ（追記専用） |
| inv_meta | スキーマ版・連番カウンタ・最終バックアップ日時 |

Record（記録）の主要フィールド: id / seq（安定ソート用連番） / timestamp / direction（in|out） / qty（常に正の整数） / itemId / itemCodeSnap / itemNameSnap / categoryIdSnap / categoryNameSnap / isManual / voided / editedAt。

差引 = Σ(入庫qty) − Σ(出庫qty)、voided==true は除外。
安定ソート = timestamp 降順 → seq 降順。
期間境界 = 開始日0:00:00 〜 終了日23:59:59.999（日付はローカルISO文字列で保持しUTC変換しない）。
スキーマは schemaVersion を持ち、将来の移行関数で旧データを変換。

---

### UI / 画面設計

calculator.html のデザイントークンを流用（色 --bg/--body/--display-bg/--gold ほか、フォント Cormorant SC / Garamond / DM Mono、角ネジ・スキャンライン・rise アニメ、ボタンの :active translateY(3px)）。

単一HTML内に5ビューを置き hidden で切替（下部タブバー）:
- 記録（entry）: display部→確認表示、btn-num→テンキー、btn-fn→入庫/出庫トグル＋種類タブ、btn-op金→確定
- 履歴（history）: 新しい順一覧＋検索＋編集/削除/取消（取消行はグレーアウト）
- 集計（summary）: グループ差引／期間集計のサブ切替
- マスタ（master）: 品番・カテゴリCRUD
- データ（data）: JSON書出/読込、CSV書出、印刷

入庫=緑系/出庫=赤系で色分け＋符号併記（誤タップ対策）。コントラストはWCAG AA以上を目標。印刷用スタイルは画面用と完全分離。

---

### 実装ステップ
1. HTML骨格（head・フォント・viewport・5ビューと下部タブバーのマークアップ）
2. デザイン移植（calculator.html の :root/背景/ネジ/display/btn/rise/scanline）＋スマホ向けレスポンシブ。印刷用 @media print は枠だけ用意
3. データ層（state構造・loadAll/save*・nextSeq・migrate・genId・保存失敗のtry/catch検知）
4. マスタCRUD（カテゴリ・品番。記録動線が依存するため先行）
5. 記録動線（種類タブ→品番タップ→テンキー→入庫/出庫→確認表示→確定、手入力＋表記ゆれ警告）※最優先体験
6. 履歴ビュー（安定ソート・編集/削除/取消＋取消ログ）
7. 絞り込み検索（品番／期間）
8. 集計ビュー（グループ差引・期間集計＋終了日境界）
9. 印刷/PDF（白地黒字の帳票風・ヘッダに期間/日時/件数）
10. データ入出力（JSON書出/読込[二段確認・取込前自動退避]・CSV書出）
11. 仕上げ（手入力品番のマスタ昇格・空/エラー/成功表示・最終バックアップ日時表示）

---

### 確認・テスト項目（手動・抜粋）
- [ ] 記録動線が実機で10秒以内に完了、入庫/出庫を選ばずに登録できない
- [ ] リロード・ブラウザ再起動後もデータが残る
- [ ] マスタ編集/削除後も過去記録が当時の品番名・種類を保持する（スナップショット）
- [ ] 差引=入庫計−出庫計、種類別合計と品番別合計の総和が一致、未分類/手入力品番も計上
- [ ] 期間集計が開始日0:00〜終了日23:59:59を含み、境界外は除外
- [ ] 履歴の編集/削除/取消が集計に反映され、取消ログに残る
- [ ] 印刷が白地黒字で崩れず、ヘッダに期間/日時/件数が出る
- [ ] JSON書出→読込で完全復元、読込時に上書き確認＋取込前自動退避
- [ ] CSVが表計算ソフトで文字化け・桁ずれなく開ける
- [ ] 数量0/負数/空欄/品番未選択で登録できない、表記ゆれ警告が出る
- [ ] モバイル幅で崩れずタップターゲットが十分、数字キーボードが出る
- [ ] LocalStorage無効/満杯/不正JSON読込でデータを壊さず警告する

---

### リスク・注意点（初版で対応必須の最重要点）
1. 保存失敗（プライベートモード・容量超過）を検知し明示する（静かな消失が最悪）
2. JSONインポートの上書き確認＋取込前の自動退避
3. 取消ログは追記専用・編集/削除は痕跡を残す（証拠性の根幹）
4. 入庫/出庫の取り違え防止（色＋符号＋確認表示）
5. 印刷は白背景反転＋ヘッダに期間/日時/件数
6. 日付はローカルISO文字列保持・終了日境界を固定
7. 外部CDN依存を最小化（フォントのみ。ロジックは依存ゼロのバニラJS）

実機での印刷崩れ・スマホUXはブラウザ差が大きく、最終的に目視確認が必要。LocalStorage・単一HTML・個人利用の構成上、暗号的な改ざん防止と多端末同期は原理的に不可（証拠性は「痕跡を残す」方向で担保）。

---

### 今回スコープ外
- ログイン・クラウド連携・多端末同期・リアルタイム共有
- 写真添付
- データのマージ取り込み（初版は全置換のみ）
- 自動バックアップ（手動のJSON書き出しで代替）
- チェックサムによる改ざん検知（証拠性ゆえ将来検討の価値あり）
- 明るい配色モードのトグル（屋外視認性は初版はコントラスト確保で対応）
