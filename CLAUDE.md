# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## プロジェクト概要

トラック運転手が社外倉庫で行う入出庫作業を自分で記録するための「第二記録アプリ」。
リフトマンが管理する帳簿との差異が生じた際に、自分の作業の正しさを証明するためのツール。

- 個人利用・単独ユーザー
- サーバー不要・単一 HTML ファイル構成
- データはブラウザの永続ストレージ（LocalStorage または IndexedDB）に保存
- ログイン・クラウド連携なし

詳細な要件・設計背景は `concept_2026-05-30.md` を参照。

---

## 技術方針

| 項目 | 方針 |
|------|------|
| 実装形式 | 単一 HTML ファイル（HTML + CSS + JS を 1 ファイルにまとめる） |
| フレームワーク | 未決定（バニラ JS または軽量ライブラリ） |
| データ保存 | LocalStorage（シンプル）または IndexedDB（大量データ対応） |
| 動作環境 | スマートフォンのブラウザ（インストール不要） |
| 印刷 | ブラウザ標準印刷機能 + `@media print` スタイル整形 |

---

## 機能スコープ（初版）

必須機能:
1. 品番マスタ登録（最大 20 件程度、追加・編集・削除）
2. 品番リストからのタップ選択（主要入力動線。10秒以内完了が最優先目標）
3. リスト外品番の手入力（例外対応）
4. 数量入力・入庫/出庫の選択
5. 日時の自動記録
6. 記録の一覧表示（新しい順）
7. 端末内へのデータ永続保存
8. 一覧の印刷・PDF 出力（ブラウザ印刷機能を利用）
9. 記録の編集・削除（打ち間違い修正用）

初版スコープ外（将来検討）:
- 品番での絞り込み検索
- 期間絞り込み印刷
- 写真添付

---

## UI 設計の制約

- 入力動線を最短にすること（品番タップ → 入庫/出庫選択 → 数量入力 → 登録）
- 片手・短時間で完了できるレイアウト（倉庫作業の合間に使用）
- タップターゲットは十分に大きく取る（スマートフォン操作前提）

---

## 開発環境

- エディタ: Cursor
- Git 管理: GitHub Desktop（CLI の `git commit` / `push` は使わない）
- ターミナル操作は極力避ける運用

単一 HTML ファイルのため、ビルドコマンド・テストコマンド・パッケージマネージャーは原則不要。
実装開始時に技術スタックが確定した場合はここに追記する。
